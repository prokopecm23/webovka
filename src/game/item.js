// Item logic for minimal roguelike
// No longer used: items are placed from level file
function createItems(level) {
  return [];
}

function itemAction(game, item, action) {
  // Placeholder for item use logic
  return null;
}

module.exports = { createItems, itemAction };
